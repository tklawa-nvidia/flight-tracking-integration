# SPDX-License-Identifier: Apache-2.0
#
# Flight Tracking Integration — sandbox network policy.
#
# The sandbox-side FastAPI server (server.py) needs outbound access to:
#   - opensky-network.org:443        (live aircraft state vectors)
#   - auth.opensky-network.org:443   (OAuth2 client_credentials token mint)
#   - services6.arcgis.com:443       (FAA AIS Special Use + Class airspace)
#   - tfr.faa.gov:443                (FAA Temporary Flight Restrictions WFS)
#
# Inference traffic (api -> Nemotron) is already governed by OpenShell's
# gateway-managed inference route configured via `openshell inference set`,
# so we don't need to declare it here.
#
# Static map tiles (openfreemap.org) are fetched directly by the user's
# browser via the openshell port-forward — they never leave the host. The
# sandbox itself does not need access to the tile server.
#
# install.sh applies the policy block below via `openshell policy set`. The
# binary path (`/usr/bin/python3`) matches the Python 3.11 interpreter that
# ships in the NemoClaw base image.

network_policies:
  flight_tracking_opensky:
    name: flight_tracking_opensky
    endpoints:
      - host: opensky-network.org
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/api/states/all" }
          - allow: { method: GET, path: "/api/states/all*" }
      - host: auth.opensky-network.org
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: POST, path: "/auth/realms/opensky-network/protocol/openid-connect/token" }
      - host: services6.arcgis.com
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/ssFJjBXIUyZDrSYZ/ArcGIS/rest/services/Special_Use_Airspace/FeatureServer/0/query*" }
          - allow: { method: GET, path: "/ssFJjBXIUyZDrSYZ/ArcGIS/rest/services/Class_Airspace/FeatureServer/0/query*" }
      - host: tfr.faa.gov
        port: 443
        protocol: rest
        enforcement: enforce
        tls: terminate
        rules:
          - allow: { method: GET, path: "/geoserver/TFR/ows*" }
    binaries:
      - { path: /usr/bin/python3 }
      - { path: /usr/bin/python3.11 }
